# KAYO

Japan lodgings, tickets and routes at one desk. Vite + React. Locales: `ja`, `en`, `zh`. Bookings are held in the browser (`localStorage`).

```
npm.cmd run dev
```

Open http://127.0.0.1:5173 — it redirects to `/ja`.
